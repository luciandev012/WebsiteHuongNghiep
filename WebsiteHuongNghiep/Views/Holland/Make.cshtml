@model IEnumerable<WebsiteHuongNghiep.Application.ViewModels.MultipleChoicesVM>

<div class="container">
    <div class="introduce">
        <p>Bước 2: Cho điểm vào từng ý trong mỗi bảng, và cộng tổng điểm của từng bảng, xác định những bảng có điểm số cao nhất</p>
        <form>
            <div class="table-name">
                @ViewBag.TableName
            </div>
            <div class="row">
                @*<div class="col-md-10 question">
                        Tôi có tính tự lập
                    </div>
                    <div class="col-md-2">
                        <input type="text" />
                    </div>*@
                <table class="table table-question">
                    <thead>
                        <tr>
                            <td>STT</td>
                            <td>Câu hỏi</td>
                            <td>Điểm</td>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(modelItem => item.Id)</td>
                                <td class="align-content-start">@Html.DisplayFor(modelItem => item.Question)</td>
                                <td><input type="text" /></td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </form>
        <a asp-action="Make" asp-route-id="1" class="btn btn-bg-light bg-info-o-400">Bảng trước</a>
        <a asp-action="Make" asp-route-id="2" class="btn btn-bg-secondary">Bảng sau</a>
    </div>
</div>>
